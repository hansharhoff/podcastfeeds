# Direct dependencies, pinned for reproducible image builds. These are the
# versions this code was developed and tested against. To upgrade, bump a pin
# and rebuild. (Transitive deps are not locked here — see the CI/lockfile TODO.)
fastapi==0.139.0
uvicorn[standard]==0.51.0
feedparser==6.0.12
trafilatura==2.1.0
edge-tts==7.2.8
mutagen==1.48.1
httpx==0.28.1
apscheduler==3.11.3
jinja2==3.1.6
pyyaml==6.0.3
sqlmodel==0.0.39
python-multipart==0.0.32
pillow==12.3.0
